services:
  - type: web
    name: whisper-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 5000
    pythonVersion: "3.9" 